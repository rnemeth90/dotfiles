# Reflector configuration — auto-ranks fastest Arch mirrors
# Service: systemctl enable reflector.timer (runs weekly)

--save /etc/pacman.d/mirrorlist
--protocol https
--country US
--latest 10
--sort rate
